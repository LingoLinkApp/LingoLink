import { languageLevels } from '@/src/constants/languages';

/*
export const transformedLanguageLevels = languageLevels.map((level) => ({
	label: level.name,
	value: level.value,
	description: level.description,
}));
*/
